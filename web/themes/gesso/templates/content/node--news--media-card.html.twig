{% set teaser = content.field_teaser.0['#context'].value %}

{% include '@components/card/card.twig'
  with {
    "teaserImage": content.field_media|field_value,
    "teaserImageAlt": content.field_media.0['#item'].alt,
    "teaserDate": content.field_published|field_value,
    "url": url,
    "title": content.title|field_value,
    "teaser": teaser|length > 150 ? teaser|slice(0, 150) ~ '...' : teaser
  }
%}
